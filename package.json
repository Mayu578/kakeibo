{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "start": "npm run build && php artisan config:clear && php artisan cache:clear && php artisan serve --host=0.0.0.0 --port=$PORT && php artisan migrate --force"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "@tailwindcss/forms": "^0.5.11",
        "tailwindcss": "^4.3.3"
    }
}
